# Upstash Redis (free at https://upstash.com) — REQUIRED for production/Vercel.
# Without these, the backend falls back to in-memory (single-machine dev only).
UPSTASH_REDIS_REST_URL=https://your-db.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_token_here
